development:
   adapter: mysql
   database: jukebox_development
   username: root
   password: <%= ENV['DBPASS'] %>
   host: localhost

test:
   adapter: mysql
   database: jukebox_test
   username: root
   password: <%= ENV['DBPASS'] %>
   host: localhost
   
production:
   adapter: mysql
   database: jukebox_production
   username: root
   password: <%= ENV['DBPASS'] %>
   host: localhost
